fastapi==0.115.0
uvicorn[standard]==0.30.6
# yt-dlp: NOT pinned. YouTube breaks stable's extractor regularly (stable 2026.6.9
# stopped paginating playlists past 100). install.sh installs the nightly channel
# (pip -U --pre "yt-dlp[default]") and a weekly systemd timer keeps it current.
yt-dlp
deep-translator==1.11.4
aiosqlite==0.20.0
python-multipart==0.0.12
pyyaml==6.0.2
aiofiles==24.1.0
telethon==1.36.0
httpx==0.27.2
# Needed by yt-dlp's DailyMotion extractor for browser-impersonation to bypass
# anti-bot checks on the per-video metadata/download request — without it every
# DailyMotion video fetch fails with "attempting impersonation... none of these
# impersonate targets are available".
curl_cffi==0.15.0
